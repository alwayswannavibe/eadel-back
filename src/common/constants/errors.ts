export enum Errors {
  PERMISSON_ERROR = "You haven't permission",
  NOT_FOUND = 'Not found',
  EMAIL_TAKEN = 'This email is already taken',
  EMAIL_OR_PASSWORD_WRONG = 'Email or password are wrong',
  FORBIDDEN_RESOURCE = 'Forbidden resource',
}
